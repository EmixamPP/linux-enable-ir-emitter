#pragma once

#include <string>
#include <vector>
using namespace std;

const string _SAVE_FOLDER_CONFIG_PATH = "@SAVE_FOLDER_CONFIG_PATH@/";

enum ExitCode
{
    SUCCESS = 0,
    FAILURE = 1,
    FILE_DESCRIPTOR_ERROR = 126,
    ROOT_REQUIRED = 2
};

string ConfigPathOf(const string &device);

vector<string> GetConfigPaths(const string &device);

string DeviceOf(const string &config_path);

vector<string> GetV4LDevices();

void CatchCtrlC();
