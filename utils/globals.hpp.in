#pragma once

#include <string>
#include <vector>
using namespace std;

const string _SAVE_FOLDER_CONFIG_PATH = "@SAVE_FOLDER_PATH@/config/";
const string _SAVE_FOLDER_SCAN_PATH = "@SAVE_FOLDER_PATH@/scan/";

enum ExitCode
{
    SUCCESS = 0,
    FAILURE = 1,
    FILE_DESCRIPTOR_ERROR = 126,
    ROOT_REQUIRED = 2
};

string deviceOf(const string &configPath);

string deviceNameOf(const string &device);

string scanPathOf(const string &device);

string configPathOf(const string &device);

vector<string> getConfigPaths(const string &device);

vector<string> getV4LDevices();
